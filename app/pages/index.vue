<template>
  <div class="india-rise-premium-portal">

    <section class="hero-cinematic-canvas">
      <div class="site-max-bounds">
        <div class="hero-split-grid">
          <div class="hero-content-pack reveal reveal-slide-up reveal-active">
            <span class="hero-eyebrow-tag">Network Commerce, Done Differently</span>
            <h1 class="hero-main-title">
              Products Worth Owning.<br />
              An Opportunity <span class="text-cobalt">Worth Building.</span>
            </h1>
            <p class="hero-paragraph">
              India Rise is a network commerce platform that brings together a portfolio of lifestyle products and a business structure designed to grow with you.
            </p>
            <div class="hero-action-cluster">
              <button class="btn-premium-cobalt">Join India Rise</button>
              <button class="btn-premium-outline">See How It Works</button>
            </div>
          </div>
          
          <div class="hero-media-wrapper reveal reveal-scale-up reveal-active">
            <div class="hero-image-frame">
              <img src="/images/hero-banner-rise.png" alt="India Rise Premium" class="hero-img" />
              <div class="hero-floating-badge">
                <span class="badge-dot"></span>
                <span class="badge-label">100% Focused on Value</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="brand-statement-section">
      <div class="site-max-bounds">
        <div class="brand-statement-wrapper reveal reveal-slide-up">
          <div class="section-meta-header">
            <span class="editorial-badge">WHAT WE ARE</span>
            <h2 class="editorial-headline">Built to Be Different from the Start</h2>
            <div class="brand-accent-line"></div>
          </div>
          
          <div class="brand-narrative-grid">
            <div class="narrative-main-card reveal reveal-slide-up delay-1">
              <p class="narrative-lead">
                India Rise was built by people who had spent enough time inside this industry to know what was not working.
              </p>
              <p class="narrative-body">
                Products that existed to be pitched, not used. Structures that rewarded recruitment over everything else. People who worked hard and still could not make sense of what they were earning or why.
              </p>
            </div>
            <div class="narrative-side-card reveal reveal-slide-up delay-2">
              <p class="narrative-body">
                We started over. Products that people want in their daily lives came first. A structure that is easy to follow came second. And from the beginning, the people building this platform with us have been treated as partners, not pipeline.
              </p>
              <div class="narrative-highlight-strip">
                If you have been in network commerce before, you will notice the difference. If you are starting fresh, you will not have to unlearn anything.
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="vision-mission-modern-canvas">
      <div class="site-max-bounds">
        <div class="section-meta-header text-center reveal reveal-slide-up">
          <span class="editorial-badge text-green">WHERE WE ARE GOING</span>
          <h2 class="editorial-headline">Our Blueprint & Purpose</h2>
        </div>

        <div class="vm-interlocking-container">
          <div class="vm-card-block vm-vision-banner reveal reveal-slide-up delay-1">
            <div class="vm-accent-label">THE VISION</div>
            <div class="vm-body-content">
              <h3>To build a platform where network commerce means something worth trusting, with products people genuinely use and an opportunity people can genuinely build on.</h3>
            </div>
          </div>

          <div class="vm-card-block vm-mission-banner reveal reveal-slide-up delay-2">
            <div class="vm-accent-label text-cobalt">THE MISSION</div>
            <div class="mission-manifesto-stack">
              <div class="mission-manifesto-row reveal reveal-slide-up delay-1">
                <span class="manifesto-num">01</span>
                <p>To offer products that add real value to people's daily lives, not just to their sales pitch.</p>
              </div>
              <div class="mission-manifesto-row reveal reveal-slide-up delay-2">
                <span class="manifesto-num">02</span>
                <p>To give every partner a clear path to income, regardless of where they start.</p>
              </div>
              <div class="mission-manifesto-row reveal reveal-slide-up delay-3">
                <span class="manifesto-num">03</span>
                <p>To run this platform in a way that people feel comfortable recommending to someone they care about.</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="principles-integrated-section">
      <div class="site-max-bounds">
        <div class="section-meta-header text-center reveal reveal-slide-up">
          <span class="editorial-badge">HOW WE OPERATE</span>
          <h2 class="editorial-headline">What We Are Built On</h2>
        </div>

        <div class="integrated-principles-flow">
          <div class="principle-unified-card brand-cobalt-strip reveal reveal-slide-up delay-1">
            <div class="unified-meta-zone">
              <span class="unified-number">01</span>
              <span class="unified-label">CORE FOUNDATION</span>
            </div>
            <div class="unified-body-zone">
              <h3>Products First</h3>
              <p>Everything here starts with the product. If someone would not buy it without the business attached, it has no place in our portfolio. That is the standard we hold ourselves to.</p>
            </div>
          </div>

          <div class="principle-unified-card brand-green-strip reveal reveal-slide-up delay-2">
            <div class="unified-meta-zone">
              <span class="unified-number">02</span>
              <span class="unified-label">OPERATIONAL CODE</span>
            </div>
            <div class="unified-body-zone">
              <h3>No Fine Print</h3>
              <p>Our compensation plan is written to be read, not decoded. We want every partner to know exactly where they stand without needing someone to explain it to them.</p>
            </div>
          </div>

          <div class="principle-unified-card clean-white-strip reveal reveal-slide-up delay-3">
            <div class="unified-meta-zone">
              <span class="unified-number text-cobalt">03</span>
              <span class="unified-label text-muted">CULTURE & ETHICS</span>
            </div>
            <div class="unified-body-zone">
              <h3>Partners, Not Headcount</h3>
              <p>The people growing India Rise with us are not a recruitment metric. They are the reason the platform exists. That shapes every decision we make about how we communicate, how we support, and how we grow.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="portfolio-panels-section">
      <div class="site-max-bounds">
        <div class="section-meta-header reveal reveal-slide-up">
          <span class="editorial-badge">THE PORTFOLIO</span>
          <h2 class="editorial-headline">Products Worth Sharing</h2>
          <p class="section-subtitle">Every category in the India Rise portfolio was chosen because it belongs in people's everyday lives, not just in a product catalogue.</p>
        </div>

        <div class="portfolio-dynamic-grid">
          <div class="portfolio-flex-item reveal reveal-slide-up delay-1">
            <div class="portfolio-media-pane">
              <img src="/images/watch.png" alt="Precision Timepieces" />
            </div>
            <div class="portfolio-text-pane">
              <h3>Precision Timepieces</h3>
              <p>Watches designed for people who value what they put on their wrist. Crafted for daily wear, not just display.</p>
            </div>
          </div>

          <div class="portfolio-flex-item reveal reveal-slide-up delay-2">
            <div class="portfolio-media-pane">
              <img src="/images/jewellery.png" alt="Personal Jewellery" />
            </div>
            <div class="portfolio-text-pane">
              <h3>Personal Jewellery</h3>
              <p>Understated and considered. Pieces made to be worn in real life rather than saved for an occasion.</p>
            </div>
          </div>

          <div class="portfolio-flex-item reveal reveal-slide-up delay-3">
            <div class="portfolio-media-pane">
              <img src="/images/beauty.png" alt="Skin and Self-Care" />
            </div>
            <div class="portfolio-text-pane">
              <h3>Skin and Self-Care</h3>
              <p>Products that earn their place in a daily routine. Focused on what they actually do, not just how they are packaged.</p>
            </div>
          </div>

          <div class="portfolio-flex-item highlight-cobalt-wrap reveal reveal-slide-up delay-4">
            <div class="portfolio-media-pane">
              <img src="/images/dining.png" alt="Table and Home" />
            </div>
            <div class="portfolio-text-pane special-cobalt-plate">
              <h3 class="text-cobalt">Table and Home</h3>
              <p>Dining and living essentials that bring a quiet sense of quality into everyday moments at home.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="compliance-dense-section">
      <div class="site-max-bounds">
        <div class="compliance-solid-box reveal reveal-scale-up">
          <div class="compliance-layout-inner">
            
            <div class="compliance-copy-block reveal reveal-slide-up delay-1">
              <span class="compliance-accent-lbl">BUILT RIGHT</span>
              <h2>Structured with the Long Term in Mind</h2>
              <p>India Rise is built in alignment with India's direct selling guidelines. Not as an afterthought, but as part of how we designed the platform from the ground up.</p>
              <p>We believe that how a platform is built eventually shows up in how it treats its people. So we chose to get that right early, before it became someone else's problem.</p>
              
              <div class="compliance-badge-inline">
                <div class="trust-badge-pill">
                  <span class="pulse-indicator"></span>
                  <span class="badge-txt">GOVERNMENT DIRECTIVE ALIGNED</span>
                </div>
              </div>
            </div>

            <div class="compliance-image-block reveal reveal-slide-up delay-2">
              <img src="https://images.unsplash.com/photo-1486406146926-c627a92ad1ab?auto=format&fit=crop&q=80&w=600" alt="Long Term Infrastructure Foundation" class="compliance-side-img" />
            </div>

          </div>
        </div>
      </div>
    </section>

    <section class="ultimate-cta-architectural">
      <div class="site-max-bounds">
        <div class="cta-shield-frame reveal reveal-slide-up">
          <div class="cta-inner-alignment reveal reveal-slide-up delay-1">
            <h2>Start Where You Are</h2>
            <p>Whether you are coming in with years of experience or completely new to this, India Rise is built to grow with you. The products are genuine. The structure is clear. The opportunity is yours to build.</p>
            <div class="cta-trigger-wrapper">
              <button class="btn-premium-cobalt size-lg">Join India Rise</button>
            </div>
          </div>
        </div>
      </div>
    </section>

  </div>
</template>

<script setup>
import { onMounted } from "vue";

onMounted(() => {
  const animatedElements = document.querySelectorAll(".reveal");

  const options = {
    root: null,
    threshold: 0.05, 
    rootMargin: "0px 0px -8% 0px" 
  };

  const scrollObserver = new IntersectionObserver((entries, observer) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add("reveal-active");
        observer.unobserve(entry.target);
      }
    });
  }, options);

  animatedElements.forEach((el) => scrollObserver.observe(el));
});
</script>

<style scoped>
@import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap');

.india-rise-premium-portal {
  /* STRICT EXECUTIVE COROPORATE COLOR SYSTEM */
  --cobalt: #003DA5;
  --cobalt-dark: #002B75;
  --cobalt-light-tint: #F3F7FF;
  --rise-green: #00B562;
  --rise-green-dark: #00914E;
  --rise-green-tint: #F0FCF6;
  
  /* CRITICAL: Strict Neutral Architecture Mapping */
  --text-main: #1A1F23;
  --text-muted: #505E6A;
  --bg-pure-white: #FFFFFF;
  --bg-structure-gray: #F5F7FA;
  --bg-structure-blue: #FAFBFD;
  --soft-border: rgba(0, 61, 165, 0.08);
  
  font-family: 'Plus Jakarta Sans', sans-serif;
  background-color: var(--bg-pure-white);
  color: var(--text-main);
  overflow-x: hidden;
  line-height: 1.55;
  width: 100%;
}

.site-max-bounds {
  width: 100%;
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 24px;
  box-sizing: border-box;
}

/* GLOBAL HEADERS WITH COMPRESSED COMPACT MARGINS */
.section-meta-header {
  margin-bottom: 24px;
  width: 100%;
}
.section-meta-header.text-center {
  text-align: center;
}
.section-subtitle {
  font-size: 15px;
  color: var(--text-muted);
  max-width: 650px;
  margin: 8px auto 0 auto;
}

.editorial-badge {
  color: var(--cobalt);
  font-weight: 800;
  font-size: 11px;
  letter-spacing: 2px;
  display: block;
  margin-bottom: 6px;
  text-transform: uppercase;
}
.editorial-badge.text-green {
  color: var(--rise-green-dark);
}

.editorial-headline {
  font-size: 32px;
  font-weight: 800;
  line-height: 1.2;
  letter-spacing: -1px;
  color: var(--text-main);
  margin: 0;
}

.text-cobalt { color: var(--cobalt); }
.text-green { color: var(--rise-green-dark); }

/* ==========================================================================
   INTERACTIVE REVEALS & MOTION 
   ========================================================================== */
.brand-statement-wrapper,
.vm-card-block,
.principle-unified-card,
.portfolio-flex-item,
.compliance-solid-box,
.cta-shield-frame {
  transition: transform 0.5s cubic-bezier(0.16, 1, 0.3, 1), 
              box-shadow 0.5s cubic-bezier(0.16, 1, 0.3, 1);
  will-change: transform, box-shadow;
}

.brand-statement-wrapper:hover,
.vm-card-block:hover,
.principle-unified-card:hover,
.portfolio-flex-item:hover,
.compliance-solid-box:hover,
.cta-shield-frame:hover {
  transform: translateY(-4px);
  box-shadow: 0 16px 32px rgba(0, 61, 165, 0.06);
}

/* ==========================================================================
   BUTTONS INTERACTIVE LAYERS
   ========================================================================== */
.btn-premium-cobalt {
  background-color: var(--cobalt);
  color: var(--bg-pure-white);
  font-weight: 700;
  font-size: 14.5px;
  padding: 13px 32px;
  border: 1px solid var(--cobalt);
  border-radius: 4px;
  cursor: pointer;
  transition: all 0.25s ease;
}
.btn-premium-cobalt:hover {
  background-color: var(--cobalt-dark);
  border-color: var(--cobalt-dark);
  box-shadow: 0 8px 20px rgba(0, 61, 165, 0.15);
}
.btn-premium-cobalt.size-lg {
  padding: 15px 40px;
  font-size: 15.5px;
}

.btn-premium-outline {
  background-color: transparent;
  color: var(--cobalt);
  font-weight: 700;
  font-size: 14.5px;
  padding: 12px 32px;
  border: 1.5px solid rgba(0, 61, 165, 0.2);
  border-radius: 4px;
  cursor: pointer;
  transition: all 0.25s ease;
}
.btn-premium-outline:hover {
  border-color: var(--cobalt);
  background-color: rgba(0, 61, 165, 0.04);
}

/* ==========================================================================
   HERO SECTION: CLEAN LIGHT LAYOUT (REDUCED PADDING)
   ========================================================================== */
.hero-cinematic-canvas {
  position: relative;
  width: 100%;
  background-color: var(--bg-pure-white);
  padding: 60px 0 45px 0;
  box-sizing: border-box;
}

.hero-split-grid {
  display: grid;
  grid-template-columns: 1.15fr 0.85fr;
  gap: 48px;
  align-items: center;
}

.hero-eyebrow-tag {
  font-size: 12px;
  font-weight: 800;
  color: var(--rise-green-dark);
  text-transform: uppercase;
  letter-spacing: 1.5px;
  display: block;
  margin-bottom: 12px;
}

.hero-main-title {
  font-size: 42px;
  font-weight: 800;
  line-height: 1.15;
  letter-spacing: -1.5px;
  color: var(--text-main);
  margin: 0 0 16px 0;
}

.hero-paragraph {
  font-size: 16px;
  color: var(--text-muted);
  margin: 0 0 24px 0;
  max-width: 580px;
}

.hero-action-cluster {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
}

.hero-media-wrapper {
  position: relative;
  width: 100%;
}

.hero-image-frame {
  position: relative;
  width: 100%;
  padding-bottom: 95%;
  border-radius: 8px;
  overflow: hidden;
  box-shadow: 0 20px 40px rgba(0, 61, 165, 0.05);
}

.hero-img {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.hero-floating-badge {
  position: absolute;
  bottom: 16px;
  left: 16px;
  background-color: var(--bg-pure-white);
  padding: 10px 18px;
  border-radius: 4px;
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.05);
  display: flex;
  align-items: center;
  gap: 8px;
  border-left: 3px solid var(--rise-green);
  z-index: 5;
}

.badge-dot {
  width: 6px;
  height: 6px;
  background-color: var(--rise-green);
  border-radius: 50%;
}

.badge-label {
  font-size: 12px;
  font-weight: 700;
  color: var(--text-main);
}

/* ==========================================================================
   BRAND STATEMENT SECTION: RECONFIGURED TINTS
   ========================================================================== */
.brand-statement-section {
  padding: 45px 0;
  background-color: var(--bg-structure-gray);
}

.brand-statement-wrapper {
  background-color: var(--bg-pure-white);
  padding: 32px;
  border-radius: 6px;
  border: 1px solid var(--soft-border);
}

.brand-accent-line {
  width: 50px;
  height: 3px;
  background-color: var(--cobalt);
  margin-top: 8px;
}

.brand-narrative-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 32px;
  margin-top: 20px;
}

.narrative-main-card .narrative-lead {
  font-size: 17px;
  font-weight: 700;
  line-height: 1.45;
  color: var(--cobalt);
  margin-bottom: 12px;
}

.narrative-body {
  font-size: 14.5px;
  color: var(--text-muted);
  margin-bottom: 12px;
}
.narrative-body:last-of-type { margin-bottom: 0; }

.narrative-highlight-strip {
  background-color: var(--bg-structure-blue);
  border-left: 3px solid var(--rise-green);
  padding: 14px;
  font-size: 14px;
  font-style: italic;
  font-weight: 600;
  color: var(--text-main);
  margin-top: 12px;
}

/* ==========================================================================
   VISION & MISSION SECTION: BALANCED STRUCTURAL BACKPLATES
   ========================================================================== */
.vision-mission-modern-canvas {
  padding: 45px 0;
  background-color: var(--bg-pure-white);
}

.vm-interlocking-container {
  display: flex;
  flex-direction: column;
  gap: 16px;
  margin-top: 20px;
}

.vm-card-block {
  padding: 32px;
  border-radius: 6px;
  border: 1px solid var(--soft-border);
}

.vm-vision-banner {
  background-color: var(--bg-structure-blue);
  border-left: 4px solid var(--cobalt);
}

.vm-vision-banner h3 {
  font-size: 16px;
  font-weight: 600;
  line-height: 1.5;
  margin: 0;
  color: var(--text-main);
}

.vm-mission-banner {
  background-color: var(--bg-structure-gray);
  border-left: 4px solid var(--rise-green);
}

.vm-accent-label {
  font-size: 14px;
  font-weight: 800;
  letter-spacing: 1.5px;
  color: var(--cobalt);
  margin-bottom: 14px;
}

.mission-manifesto-stack {
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.mission-manifesto-row {
  display: flex;
  gap: 14px;
  align-items: flex-start;
}

.manifesto-num {
  font-size: 13px;
  font-weight: 800;
  color: var(--bg-pure-white);
  background-color: var(--cobalt);
  min-width: 28px;
  height: 28px;
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.mission-manifesto-row p {
  margin: 0;
  font-size: 14.5px;
  font-weight: 500;
  color: var(--text-main);
  padding-top: 2px;
}

/* ==========================================================================
   PRINCIPLES SECTION: UNIFIED MATRICES (NO CHARCOAL)
   ========================================================================== */
.principles-integrated-section {
  padding: 45px 0;
  background-color: var(--bg-structure-blue);
}

.integrated-principles-flow {
  display: flex;
  flex-direction: column;
  gap: 14px;
  margin-top: 20px;
}

.principle-unified-card {
  display: grid;
  grid-template-columns: 0.45fr 1.55fr;
  align-items: center;
  padding: 24px 32px;
  border-radius: 6px;
  background-color: var(--bg-pure-white);
  border: 1px solid var(--soft-border);
}

.unified-meta-zone {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.unified-number {
  font-size: 28px;
  font-weight: 800;
  line-height: 1;
}

.unified-label {
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 1px;
  color: var(--text-muted);
}

.unified-body-zone h3 {
  font-size: 18px;
  font-weight: 700;
  margin: 0 0 6px 0;
  color: var(--text-main);
}

.unified-body-zone p {
  font-size: 14px;
  line-height: 1.5;
  margin: 0;
  color: var(--text-muted);
}

.brand-cobalt-strip {
  border-left: 4px solid var(--cobalt);
}
.brand-cobalt-strip .unified-number { color: var(--cobalt); }

.brand-green-strip {
  border-left: 4px solid var(--rise-green);
}
.brand-green-strip .unified-number { color: var(--rise-green-dark); }

.clean-white-strip {
  border-left: 4px solid #A0AAB2;
}
.clean-white-strip .unified-number { color: var(--text-main); }

/* ==========================================================================
   PRODUCT PORTFOLIO SECTION: FIXED OVERLAPS (GRID TILES)
   ========================================================================== */
.portfolio-panels-section {
  padding: 45px 0;
  background-color: var(--bg-pure-white);
}

.portfolio-dynamic-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
  margin-top: 20px;
}

.portfolio-flex-item {
  background-color: var(--bg-structure-gray);
  border-radius: 6px;
  overflow: hidden;
  border: 1px solid var(--soft-border);
  display: flex;
  width: 100%;
  min-height: 150px;
}

.portfolio-media-pane {
  width: 35%;
  position: relative;
  flex-shrink: 0;
}

.portfolio-media-pane img {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.portfolio-text-pane {
  width: 65%;
  padding: 24px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  box-sizing: border-box;
  background-color: var(--bg-pure-white); /* Guarantees pure separation on backgrounds */
}

.portfolio-text-pane h3 {
  font-size: 18px;
  font-weight: 700;
  color: var(--text-main);
  margin: 0 0 6px 0;
}

.portfolio-text-pane p {
  font-size: 13.5px;
  color: var(--text-muted);
  margin: 0;
  line-height: 1.5;
}

.highlight-cobalt-wrap {
  border: 1px solid rgba(0, 61, 165, 0.15);
}
.special-cobalt-plate {
  background-color: var(--cobalt-light-tint);
}

/* ==========================================================================
   COMPLIANCE SECTION: PREMIUM BRAND PLATE
   ========================================================================== */
.compliance-dense-section {
  padding: 40px 0;
  background-color: var(--bg-structure-gray);
}

.compliance-solid-box {
  background-color: var(--bg-pure-white);
  color: var(--text-main);
  border-radius: 8px;
  padding: 32px;
  border: 1px solid var(--soft-border);
}

.compliance-layout-inner {
  display: grid;
  grid-template-columns: 1.15fr 0.85fr;
  gap: 40px;
  align-items: center;
}

.compliance-accent-lbl {
  font-size: 11px;
  font-weight: 800;
  color: var(--rise-green-dark);
  letter-spacing: 1.5px;
  display: block;
  margin-bottom: 8px;
}

.compliance-copy-block h2 {
  font-size: 26px;
  font-weight: 800;
  margin: 0 0 12px 0;
  line-height: 1.25;
  color: var(--text-main);
}

.compliance-copy-block p {
  font-size: 14px;
  color: var(--text-muted);
  margin: 0 0 14px 0;
  line-height: 1.55;
}
.compliance-copy-block p:last-of-type { 
  margin-bottom: 20px; 
}

.compliance-badge-inline {
  display: flex;
}

.trust-badge-pill {
  display: flex;
  align-items: center;
  gap: 8px;
  background-color: var(--rise-green-tint);
  border: 1px solid rgba(0, 181, 98, 0.2);
  padding: 8px 16px;
  border-radius: 4px;
}

.pulse-indicator {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background-color: var(--rise-green);
}

.badge-txt {
  font-size: 11px;
  font-weight: 800;
  color: var(--rise-green-dark);
  letter-spacing: 0.5px;
}

.compliance-image-block {
  width: 100%;
  height: 240px;
  border-radius: 6px;
  overflow: hidden;
  box-shadow: 0 10px 25px rgba(0,61,165,0.04);
}

.compliance-side-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

/* ==========================================================================
   LAST CTA SECTION: SOLID OUTLINE EXECUTION
   ========================================================================== */
.ultimate-cta-architectural {
  padding: 45px 0 60px 0;
  background-color: var(--bg-pure-white);
}

.cta-shield-frame {
  background-color: var(--bg-structure-blue);
  border: 1px solid var(--soft-border);
  border-top: 4px solid var(--cobalt);
  border-radius: 6px;
  padding: 36px;
  max-width: 850px;
  margin: 0 auto;
}

.cta-inner-alignment {
  text-align: center;
  max-width: 680px;
  margin: 0 auto;
}

.cta-inner-alignment h2 {
  font-size: 28px;
  font-weight: 800;
  color: var(--text-main);
  margin: 0 0 10px 0;
  letter-spacing: -0.5px;
}

.cta-inner-alignment p {
  font-size: 14.5px;
  color: var(--text-muted);
  margin: 0 0 20px 0;
}

.cta-trigger-wrapper {
  display: flex;
  justify-content: center;
}

/* ==========================================================================
   SCROLL REVEAL UTILITIES
   ========================================================================== */
.reveal {
  opacity: 0;
  will-change: transform, opacity;
  transition: opacity 0.9s cubic-bezier(0.16, 1, 0.3, 1), 
              transform 0.9s cubic-bezier(0.16, 1, 0.3, 1);
}

.reveal-slide-up { transform: translateY(24px); }
.reveal-scale-up { transform: scale(0.98); }

.reveal.reveal-active {
  opacity: 1;
  transform: translateY(0) scale(1);
}

.reveal.delay-1 { transition-delay: 0.1s; }
.reveal.delay-2 { transition-delay: 0.2s; }
.reveal.delay-3 { transition-delay: 0.3s; }
.reveal.delay-4 { transition-delay: 0.4s; }

/* ==========================================================================
   RESPONSIVE LAYOUT MATRIX
   ========================================================================== */
@media (max-width: 960px) {
  .hero-split-grid {
    grid-template-columns: 1fr;
    gap: 32px;
  }
  .hero-media-wrapper {
    order: -1;
  }
  .hero-image-frame {
    padding-bottom: 60%;
  }
  .brand-narrative-grid {
    grid-template-columns: 1fr;
    gap: 16px;
  }
  .principle-unified-card {
    grid-template-columns: 1fr;
    gap: 8px;
    padding: 20px;
  }
  .portfolio-dynamic-grid {
    grid-template-columns: 1fr;
    gap: 16px;
  }
  .compliance-layout-inner {
    grid-template-columns: 1fr;
    gap: 24px;
  }
  .compliance-image-block {
    height: 200px;
  }
}

@media (max-width: 640px) {
  .hero-main-title { font-size: 32px; }
  .editorial-headline { font-size: 24px; }
  
  .brand-statement-wrapper, .vm-card-block, .cta-shield-frame, .compliance-solid-box {
    padding: 20px;
  }
  
  .compliance-copy-block h2 { font-size: 20px; }
  .cta-inner-alignment h2 { font-size: 22px; }
  
  .hero-action-cluster .btn-premium-cobalt, 
  .hero-action-cluster .btn-premium-outline {
    width: 100%;
    text-align: center;
  }
}
</style>